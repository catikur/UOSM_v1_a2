{
  "name": "uosm-pet-ct-sgk-rehberi",
  "version": "2.0.0",
  "description": "SUT temelli PET/BT SGK uygunluk karar destek rehberi - veri odakli, parametrik ve otomatik guncellenebilir.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:build": "node scripts/build.mjs --check",
    "check:docs": "node scripts/gen-docs.mjs --check",
    "check:updates": "node scripts/check-updates.mjs",
    "docs": "node scripts/gen-docs.mjs",
    "fetch:sources": "node scripts/fetch-sources.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "validate": "node scripts/validate.mjs",
    "verify": "npm run validate && npm test && npm run check:build && npm run check:docs"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/catikur/UOSM_v1_a2.git"
  }
}
